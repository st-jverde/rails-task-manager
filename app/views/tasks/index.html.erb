<h1 class="font-bold text-2xl">My Tasks</h1>

<ul>
  <% @tasks.each do |task| %>
    <li><%= check_box_tag task.completed%><%= link_to task.title, task_path(task) %></li>
  <% end %>
</ul>

<%= link_to "Add a new task", '/tasks/new' %>
